# Capability matrix

“Tested” means local automated or synthetic coverage exists. It does not mean production authorization. “Safe to run” is intentionally narrower than “implemented.” Commands containing angle-bracket placeholders are signatures, not ready-to-run production commands.

| Capability | CLI command | Implemented | Tested | Independently audited | Safe to run | Writes data | Uses GPU | Uses provider credits |
|---|---|---|---|---|---|---|---|---|
| Unlabeled pool verification | `python -m spritelab.unlabeled_pool verify --pool datasets/sprite_lab_unlabeled_pool_v1_r2` | Yes | Yes; current read-only verification passes | Yes; frozen-r2 identity has prior read-only audit coverage | Yes; read-only | No | No | No |
| Dataset-v5 preview verification | `python -m spritelab.dataset_v5.cli verify-policy-preview --dataset datasets/sprite_lab_multisource_v5_policy_v2_core_plus_weighted_sampling_preview` | Yes | Yes; current read-only verification passes | Yes for the preview verifier and preview only | Yes; read-only, but does not authorize production | No | No | No |
| Dataset-v5 named-view synthetic build | `python -m spritelab.dataset_v5.cli build-view --contract-root <contract> --view <name> --policy <policy> --source-manifest <synthetic.jsonl> --output <new-temp-root>` | Yes; seven views | Yes; synthetic only | No; fresh builder audit required | Synthetic OS-temporary fixtures only | Yes | No | No |
| Labeling-v4 quality GUI | `python -m spritelab harvest assisted-v4 --records <records> --corrections <events> --mode quality-only` | Yes | Yes; mocked/synthetic | No; fresh audit required | No for real Wave-1 review | Yes; append-only events | No | No |
| Labeling-v4 queue freeze | `python -m spritelab harvest label-v4-freeze-inference-queue --audit-selection <selection> --prefilled-records <prefill> --human-truth <truth> --output-root <new-root>` | Yes | Yes; synthetic repeated freeze | No; fresh audit required | Synthetic OS-temporary fixtures only; real freeze blocked | Yes | No | No |
| Labeling-v4 semantic preparation | `python -m spritelab harvest label-v4-prepare-audit --inference-queue <queue> --output-root <new-root> --inference-policy cached-only` | Yes | Yes; synthetic/cached paths | No; fresh audit required | Synthetic or cached diagnostics only; real preparation blocked | Yes | No | No in cached mode; explicit provider mode would use credits and is blocked |
| Memorization scoring | `python -m spritelab eval score-suite --generated <existing-artifacts> --out <new-root> --training-manifest <manifest>` | Yes | Yes; isolated CPU cases | No; combined integration audit fails | Synthetic/existing diagnostic artifacts only; not promotion evidence | Yes | No; CPU scorer | No |
| Memorization review | `python -m spritelab eval review-memorization --report <report> --out <new-root>` | Yes | Yes; isolated schema cases | No; combined integration audit fails | No real human review in this milestone | Yes; append-only reviews | No | No |
| Promotion decision | `python -m spritelab eval promotion-decision <bound-evidence-arguments> --out <new-root>` | Yes | Yes; isolated cases | No; combined integration audit fails | Synthetic/historical diagnostics only; cannot authorize promotion | Yes; decision report only | No | No |
| Training campaign plan | `python -m spritelab train campaign-plan --config <candidate.json> --out <new-manifest.json> --plan-only` | Yes | Yes; synthetic | No; v2 audit fails closed on unbound identities | Synthetic diagnostics only | Yes | No | No |
| Training campaign validate | `python -m spritelab train campaign-validate --campaign <manifest.json> --validate-only` | Yes | Yes; synthetic | No; v2 audit fails closed on unbound identities | Synthetic diagnostics only; cannot authorize execution | No unless `--out` is supplied | No | No |
| Training campaign status | `python -m spritelab train campaign-status --campaign <manifest.json>` | Yes | Yes; synthetic | No; v2 audit fails closed on unbound identities | Synthetic diagnostics only | No unless `--out` is supplied | No | No |
| Training campaign run | `python -m spritelab train campaign-run --campaign <manifest.json> --execute --confirm-execute` | Yes | Launch seams tested locally | No; v2 audit fails closed on unbound identities | No; real execution blocked | Yes | Yes for intended training | No |

The `0.20` raking result is a candidate, not an approved production policy. Preview verification does not change that status.
